\section{一维随机变量及其分布}
\subsection{随机变量与分布函数的概念}
\begin{mydef}{随机变量}{random variable}
随机变量是随机试验的结果，是一个随机试验的函数。
设随机试验E的样本空间为$\Omega, \omega \in R$若对任意实数$x$,事件
$
\{X \leq x\} = \{\omega | X(\omega) \leq x \}
$
\end{mydef}

\begin{mydef}{分布函数}{distribution function}
$X$是随机变量,$x$是任意实数,$F(X) = P(X \leq x)$是随机变量$X$的
分布函数。称$X$服从$F(X)$分布或$X \sim F(X)$
\end{mydef}

\begin{conclusion}{分布函数的性质}{distribution function properties}
\begin{enumerate}
    \item 分布函数是单调不减函数。
    \item $0 \leq F(X) \leq 1$。$F(-\infty) = 0, F(\infty) = 1$。
    \item $F(X)$是右连续函数。$x_{0} \in R$时，$F(x_{0})$存在，且有
    $$
    \lim_{x \to {x_{0}^{+}}} F(x) = F(x_{0} + 0) = F(x_{0})
    $$
    \item 对任意$x_{1} < x_{2}, P\{x_{1} \leq X \leq x_{2}\} = F(x_{2}) - F(x_{1})$
    \item 对任意$x_{0} \in R$, $P(X = x_{0}) = F(x_{0}) - F(x_{0} - 0)$
    \item 设$x_{1} < x_{2}$,且$F(X)$在$x_{1},x_{2}$处连续，$P\{x_{1} \leq X \leq x_{2}\} = F(x_{2}) - F(x_{1})$
\end{enumerate}
\end{conclusion}

\subsection{离散型随机变量及连续型随机变量}
\begin{mydef}{离散型随机变量}{discrete random variable}
    如果随机变量$X$只可能取有限个或无限个离散的值，则为离散型随机变量。
    $X$的概率分布(或分布律，分布列)可表示为
    $$
    P(X = x_{i}) = p_{i}, i = 1,2,...,n
    $$
    或表格形式
    $$
    \begin{tabular}{c|c}
% \toprule
\hline
$X$ & $x_{1} \quad x_{2} \quad x_{3} \quad \ldots \quad x_{n}$ \\
% \midrule
\hline
$P$ & $p_{1} \quad p_{2} \quad p_{3} \quad \ldots \quad p_{n}$ \\
% \bottomrule
\hline
    \end{tabular}
    $$
    或
    $$
    \begin{aligned}
        X \sim
        %大圆括号
        \left(
            \begin{array}{llll}
                x_{1} & x_{2} & \ldots & x_{n} \\
                p_{1} & p_{2} & \ldots & p_{n} \\
            \end{array}
        \right)
    \end{aligned}
    $$
\end{mydef}

\begin{mydef}{连续型随机变量}{continuous random variable}
    若随机变量$X$的分布函数$F(X)$，存在非负可积函数$f(x)$，使得
    $$
    F(x) = P(X \leq x) = \int_{-\infty}^{x} f(t) dt
    $$
    则称$X$为连续型随机变量，$f(x)$为$X$的概率密度函数。\\
    \\
    \boxed{\textbf{注}} 随机变量的取值并不可列，转换为F(X)求得是落入某一区间的概率。$f(x)$是密度值，而不是概率值


\end{mydef}

\subsection{离散型随机变量及其分布}
\begin{mydef}{离散型随机变量的分布函数}{distribution function of discrete random variable}
    设$X$是离散型随机变量，$x_{1},x_{2},...,x_{n}$是$X$的可能取值，
    $P(X = x_{i})$是$X$取$x_{i}$的概率，$F(X)$是$X$的分布函数，
    则有
    $$
    F(X) = P(X \leq x) = P(X = x_{1}) + P(X = x_{2}) + ... + P(X = x_{n})
    $$
\end{mydef}

\begin{tablebox}{常见离散型随机变量分布}
    \centering
    \scalebox{1.2}{
    \begin{tabular}{|c|c|c|}
        \hline
        \text{名字} & 符号 & 概率分布 \\
        \hline
        0-1分布 & $P(X = 0)$ & $P(X = 0) = 1 - P(X = 1)$ \\
        \hline
        二项分布 & $B(n,p)$ & $P(X = k) = C_{n}^{k} p^{k} (1 - p)^{n - k}$ \\
        \hline
        泊松分布 & $P(\lambda)$ & $P(X = k) = \frac{\lambda^{k} e^{-\lambda}}{k!}$ \\
        \hline
        几何分布 & $G(p)$ & $P(X = k) = p (1 - p)^{k - 1}$ \\
        \hline
        超几何分布 & $H(M,N,n)$ & $P(X = k) = \frac{C_{k}^{n} C_{M - k}^{N - n}}{C_{M}^{N}}$ \\
        \hline
    \end{tabular}
    }
    \label{tab:discrete_random_variable}
\end{tablebox}


\subsection{连续型随机变量及其分布}
\begin{mydef}{连续型随机变量的分布函数}{distribution function of continuous random variable}
    设$X$是连续型随机变量，$f(x)$是$X$的概率密度函数，$F(X)$是$X$的分布函数，
    则有
    $$
    F(X) = P(X \leq x) = \int_{-\infty}^{x} f(t) dt
    $$
\end{mydef}

\begin{tablebox}{常见连续型随机变量分布}
    %对齐左边
    % \raggedleft
    \centering
    \scalebox{0.94}{
    \begin{tabular}{|p{1.2cm}|c|c|c|}
        \hline
        \text{名字} & 符号 & 分布函数 & 概率密度函数 \\
        \hline
        均匀分布 & 
        $
        U(a,b)$ & $F(x) = \begin{cases}
            0, & x < a \\
            \frac{x - a}{b - a}, & a \leq x \leq b \\
            1, & x > b \\
        \end{cases}
        $ 
        & 
        $
        f(x) = \begin{cases}
            \frac{1}{b - a}, & a < x < b \\
            0, & \text{其他}
        \end{cases}
        $ \\
        \hline
        指数分布 & 
        $
        \begin{aligned}
            & ~~ E(\lambda)  \\
            &(\lambda > 0) \\
        \end{aligned}
        $
        & $F(X) = \begin{cases}

            1 - \mathrm{e}^{-\lambda x},& x \geq 0 \\
            0, & x < 0 \\
        \end{cases}$ & $f(x) = \begin{cases}
            \lambda \mathrm{e}^{-\lambda x}, & x > 0 \\
            0, & \text{其他}
        \end{cases}$ \\
        \hline
        正态分布 & $N(\mu,\sigma)$ & 
         $\Phi(X)$
        & 
        $
        \begin{aligned}
            \phi(x)&=f(x) = \frac{1}{\sigma \sqrt{2\pi}} \mathrm{e}^{-\frac{(x - \mu)^{2}}{2 \sigma^{2}}} \\
            &\quad (-\infty < x < \infty)
        \end{aligned}
        $ \\
        \hline
    \end{tabular}
    }
    \label{tab:continuous_random_variable}
\end{tablebox}

\begin{conclusion}{标准正态分布}{standard normal distribution}
    标准正态分布是正态分布的标准化，即$N(0,1)$，概率密度函数为
    $$
    \phi(x) = \frac{1}{\sqrt{2\pi}} \mathrm{e}^{-\frac{x^{2}}{2}}
    $$
    其分布函数为
    $$
    \Phi(x) = \frac{1}{\sqrt{2\pi}} \int_{-\infty}{x}\mathrm{e}^{-\frac{t^2}{2}}\mathrm{d}t
    $$
    $\phi(x)$为偶函数，则$\Phi(0) = 0.5$, $\Phi(-x) = 1 - \Phi(x)$
    当$a > 0$时，
    $$
    \begin{aligned}
        P(a < X < b) &= \Phi(b) - \Phi(a) \\
        P(|x| \le a) &= 2\Phi(a) -1
    \end{aligned}
    $$
\end{conclusion}

\begin{conclusion}{正态分布的标准化}{standardlize normal distribution}
    设$X$为正态分布，$X \sim N(\mu, \sigma^{2})$, $Y = \frac{X - \mu}{\sigma}$，则$Y$为标准正态分布。\\
    $$
    % \begin{aligned}
        P(a < X < b) = P(\frac{a - \mu}{\sigma} < Y < \frac{b - \mu}{\sigma})
        =\Phi(\frac{b - \mu}{\sigma}) - \Phi(\frac{a - \mu}{\sigma})
    % \end{aligned}
    $$
\end{conclusion}

\subsection{一维随机变量函数的分布}
\begin{mydef}{随机变量的函数}{function of random variable}
    设$X$为随机变量，$Y = f(X)$为$X$的函数，$Y$也为随机变量，$Y$的分布函数为$F(Y)$，
    则有
    $$
    F(Y) = P(Y \leq y) = P(f(X) \leq y) = P(X \leq f^{-1}(y))
    $$
\end{mydef}

\begin{conclusion}{离散型随机变量函数的分布}{distribution of discrete random variable function}
    设$X$为离散型随机变量，$Y = f(X)$为$X$的函数，$Y$也为离散型随机变量，
    求$X$的分布律的步骤为
    \begin{enumerate}
        \item 求$Y=g(X)$的所有可能的取值$a_{1}, a_{2}, \cdots, a_{n}$ 
        \item 计算$P(Y = a_{i})$，若$a_{i}$相同，合并其概率值
    \end{enumerate}
\end{conclusion}

\begin{conclusion}{连续型随机变量函数的分布}{distribution of continuous random variable function}
    设$X$为连续型随机变量，$Y = f(X)$为$X$的函数，$Y$也为连续型随机变量，
    求$X$的分布律有两种方法
    \begin{enumerate}
        \item 定义法(分布函数法)\\
        设$Y = f(X)$的分布函数为$F(Y)$，则有
        $$
        \begin{aligned}
        F(Y) &= P(Y \leq y) \\
        &= P(f(X) \leq y) = P(X \leq f^{-1}(y))\\
        &= \int_{g(x) \leq y}^{} f_{X}x\mathrm{d}x \\
        \end{aligned}
        $$
        $Y = f(X)$的概率密度函数为$f(y)$为
        $$
        f(y) = \frac{\mathrm{d}F_{Y}(y)}{\mathrm{d}y}
        $$
        \item 若$g^{'}(x) \ne 0$,$y=g(x)$与其反函数$x=h(y)$单调性一致。\\
        故 \\ 
        $$
        \begin{aligned}
        F_{Y}(y) = P(Y \leq y) &= P(g(x) \leq y) = P(X \geq h(y)) \\
        f_{Y}(y) &= F^{'}_{Y}(y) = f_{X}(h(y))|h^{'}(y)|
        \end{aligned}
        $$
        $$
        f_{Y}(y) = 
        \begin{cases}
            f_{X}(h(y))|h^{'}(y)|, & \alpha < y < \beta \\
            0, & \text{其他}
        \end{cases}
        $$

    \end{enumerate}

\end{conclusion}

\subsection{一些其他技巧}
\begin{conclusion}{泊松积分}{poisson integral}
$$
    \int_{-\infty}^{\infty} \mathrm{e}^{-t^2} = \sqrt{\pi}
$$
\end{conclusion}

\begin{conclusion}{泊松分布下的比例问题}
    设$X \sim P(\lambda)$，则$P(X = k) = \frac{\lambda^k e^{-\lambda}}{k!}$ \\
    在$X$中发生$Y$的比例为$\alpha$，\\
    $Y$的分布为$P(\alpha \lambda)$ ,   
    则$P(Y = k) = \frac{(\lambda \alpha)^k e^{-\lambda \alpha}}{k!}$
\end{conclusion}

