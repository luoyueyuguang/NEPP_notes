# 408 计算机学科专业基础综合讲义审阅报告

审阅日期：2026-07-22

## 1. 结论先行

经过首轮整改和第二轮独立复审后，讲义的四科主干知识、章节组织和三套模拟卷的试卷结构已达到可使用状态；两轮能够确认的知识性错误、答案硬伤、多答案/无正确选项、题干条件不足、缺图依赖、错误综合时间线和致命构建问题均已修正或撤下。

当前正式内容与截至审阅日可确认的、稳定施行的 408 科目框架及近年统考试卷结构一致：

- 40 道单项选择题，每题 2 分，共 80 分；
- 7 道综合应用题，共 70 分；
- 满分 150 分，考试时间 180 分钟；
- 数据结构 45 分、计算机组成原理 45 分、操作系统 35 分、计算机网络 25 分。

需要同时保留三个边界：

1. 本报告不能证明整套 428 页材料“绝无错误”。结论是：两轮确认出的硬伤已清除，静态检查和完整构建通过，并建立了后续发布门槛。
2. 原有精确真题年份/题号没有完成逐卷权威核验，已从正式讲义中隔离；正文题目统一按“真题风格”发布，不能把它们当作正式真题索引。
3. 截至审阅日不能把材料表述为“已与尚未发布的 2027 新大纲完全一致”。2027 新大纲发布后仍须做一次增量复核。

综合判断：**可以作为当前 408 系统复习讲义、章节训练和模拟训练使用；模拟卷结构、科目分值、主干覆盖和基本题风已对齐，每套又补入 4 道中等或多步骤选择题，难度分布较首轮明显改善，但仍不应宣传为与某一年度正式卷完全等难。**

## 2. 审阅范围、基准与方法

### 2.1 正式审阅范围

本轮审阅对象为 `408/` 下进入正式成书流程的内容：

- 数据结构：`ds/chap1.tex`—`ds/chap8.tex`；
- 计算机组成原理：`co/chap1.tex`—`co/chap7.tex`；
- 操作系统：`os/chap1.tex`—`os/chap6.tex`；
- 计算机网络：`cn/chap1.tex`—`cn/chap6.tex`；
- 三套模拟卷：`exams.tex`；
- 成书入口与审计说明：`main.tex`、`appendix-audit.tex`；
- 最终 XeLaTeX 构建产物与日志。

`appendix-exams.tex` 中的精确年份/题号尚未完成权威逐卷核验，因此已经从 `main.tex` 的正式输入链移除，不属于本次可发布真题索引。

### 2.2 审阅基准

- **范围基准**：数据结构、计算机组成原理、操作系统、计算机网络四科的稳定主干框架；
- **结构基准**：40 道单选 + 7 道综合应用题，150 分，180 分钟，四科分值 45/45/35/25；
- **题型基准**：单选题强调概念辨析、条件判断、过程模拟和小规模计算；综合题强调多步骤推导、算法/过程描述和跨知识点建模；
- **质量基准**：题干条件充分、答案唯一、选项可判定、解析与答案一致、公式和代码边界明确、图表不依赖缺失素材；
- **发布基准**：不得把未经逐卷核验的年份、题号和题源归属作为事实发布。

### 2.3 审阅方法

本轮采用仓库级审阅，而不是只抽查少量页面：

1. 对四科高风险计算、算法过程、协议状态和系统综合题重新推演；
2. 检查选择题是否存在多答案、无正确答案或条件不足；
3. 检查综合题题干信息是否足以推出答案，解析是否覆盖关键步骤；
4. 核对三套模拟卷的题数、选择题答案数、分值和四科配比；
5. 扫描 TODO、待核实、草稿式自我否定、未经核验的年份/题号和过强断言；
6. 连续完整构建并检查致命错误、交叉引用、缺字、未定义命令和严重横向溢出。

这套方法能够显著降低知识和工程质量风险，但不能替代官方大纲发布后的逐条对照，也不能替代精确真题元数据的逐卷查验。

## 3. 修订前的发布阻断项

修订前存在的问题不是单纯排版瑕疵，若直接发布会实质影响学习：

| 类别 | 典型问题 | 处理结果 |
|---|---|---|
| 答案硬伤 | 页面置换、Cache、Huffman、AVL、流水线、TCP、CRC、KMP、散列等题的结果或过程有误 | 已重新计算并修正 |
| 选择题唯一性 | 个别题存在两个正确选项、无正确选项或题干条件不足 | 已改写题干/选项；无法唯一判断的题明确改为“信息不足” |
| 综合题建模 | 多进程时间线允许未获 CPU 的进程访问页面，并漏掉后续引用 | 错误时间线已撤下，改为跨模块时间线的建模规则 |
| 缺失素材 | 个别题依赖未提供的图，无法独立作答 | 改为自包含文字题或补全必要条件 |
| 复杂度不匹配 | 模拟卷要求“尽可能高效”排序，解析却给出 \(O(n^2)\) 插入排序 | 改为链表自底向上归并排序，时间 \(O(n\log n)\)、辅助空间 \(O(1)\) |
| 元数据风险 | 大量年份/题号未经权威逐卷核验，却被当作真题来源发布 | 正文降级为“真题风格”；原索引从正式构建隔离 |
| 覆盖缺口 | 端序/对齐/校验、线程/IPC/管程、ARQ/802.11/VLAN/NAT 等内容不足 | 已补入相应章节 |
| 工程质量 | 构建曾有致命错误、缺字、宽表严重溢出和草稿残留 | 致命问题已清除，宽表和长公式已集中整改 |

## 4. 本轮主要知识性修订

以下是影响正确性或学习边界的代表性修订，不是逐行变更日志。

### 4.1 数据结构

- 补齐数组地址计算、行/列优先存储、特殊矩阵和稀疏矩阵；
- 修正 Huffman 合并代价、编码一致性、WPL 与概率未归一化时的解释；
- 修正 AVL 插入序列的旋转类型和最终树形；
- 修正遍历重构题的唯一答案，删除“查原题”等草稿痕迹；
- 修正拓扑序、Dijkstra、AOE 网和关键路径相关题解；
- 统一 B 树/B+树术语，修正 B 树高度、删除过程和散列表 ASL；
- 明确二次探测覆盖结论的适用形式与模数条件；
- 明确图遍历序列的唯一性依赖起点和邻接点扫描顺序；
- 将链表高效排序改为自底向上归并排序。

### 4.2 计算机组成原理

- 补齐 ASCII、8421 BCD、大小端、边界对齐、奇偶校验、海明码、SEC-DED 和多类移位；
- 修正补码、IEEE 754、Booth 乘法、流水线、Cache、微程序编码和扩展操作码题解；
- 修正 DRAM 集中刷新、分散刷新和异步刷新的定义；
- 不再把精确 LRU 说成高相联硬件中普遍采用的实现，补充伪 LRU/随机替换边界；
- Cache 映射统一为直接映射、组相联和全相联三类；
- 区分 Fault、Trap、Abort 的返回语义，不再把异常笼统称为“软件中断”；
- 修正 DMA 周期挪用、总线竞争和“完全并行”的绝对化表述；
- 修正指令周期表述：间址和中断周期并非每条指令必然经历；
- 对除零异常的 Fault/Abort 分类增加体系结构边界；
- 将多处宽表和长公式改成可换行布局。

### 4.3 操作系统

- 补齐线程模型、IPC、Peterson、TestAndSet、Swap、CAS 和管程；
- 修正银行家算法资源总量、Need/Available 计算和安全性判断；
- 对 Max/Need 信息不完整的综合题明确结论为“信息不足”，不再强行给出安全序列；
- 重新计算页面访问串：3 个页框时 OPT=11、FIFO=16、LRU=15，4 个页框时 FIFO=14；该串不构成 3→4 页框的 Belady 异常；
- 明确缺页有效访问时间公式中缺页处理时间是否包含重新执行后的访存；
- 修正动态分区最佳适应算法的局部判据，不再声称其全局外部碎片最小；
- 修正 FAT 随机访问表述，区分链接分配与索引分配；
- 修正字符设备缓存、DMA、单/双缓冲和多类 PV 题；
- 修正多实例资源分配图的剩余资源和可化简序列；
- 修正 i-node 最大文件大小、二级间接区偏移和磁盘平均访问时间；
- 明确 `MAP_SHARED` 与 `MAP_PRIVATE` 的写回/COW 语义；
- 改写错误的跨模块进程—页面—I/O 时间线，列出完成此类题所需的全部时序条件。

### 4.4 计算机网络

- 补齐停止—等待、GBN、SR、ALOHA、CSMA、CSMA/CA、802.11、隐藏站、RTS/CTS、VLAN、NAT/NAPT/PAT 和私网地址；
- 修正以太网最短帧、子网划分、HTTP RTT、DNS 递归/迭代、SMTP/MIME/端口和 NAT 校验和；
- 区分 408 教材化拥塞窗口规则与 TCP Reno 快速恢复规则；
- DNS `hosts` 文件不再被称为 DNS 缓存；
- CSMA/CD 的短帧问题改为“无法保证发送期间检测到冲突”，不再误说成“无法重传”；
- 曼彻斯特编码的跳变方向明确以题目约定为准，并限定经典 10 Mb/s 以太网语境；
- 修正 IPv4 分类前缀位、DSCP/ECN、TTL 初始值和 IPv6/NAT 的绝对化表述；
- 修正 POP3/IMAP 的服务器副本、文件夹和离线缓存语义；
- 删除或弱化会迅速过时的固定介质速率/距离数字，保留稳定的比较关系。

## 5. 第二轮独立复审新增发现与处理

第二轮没有把首轮结论当作既定事实，而是重新检查高风险定义、适用条件、算法复杂度、系统过程题和三套模拟卷答案。新增发现和整改如下。

### 5.1 数据结构

- 循环链表只能方便地从当前结点沿一个方向循环访问，任意查找仍通常为 \(O(n)\)；删除“任意位置快速到达任意位置”的错误表述。
- 子孙结点定义明确排除结点自身，避免把“后代”和“自身或后代”混用。
- 任意二叉树的非递归遍历辅助空间统一表述为 \(O(h)\)，只有在树高 \(h=O(\log n)\) 时才可写成 \(O(\log n)\)。
- 原并查集代码实际按集合大小合并，却标为“按秩合并”；现已区分 rank 与 size，并将代码、变量和解释统一为按大小合并。
- Shell 排序复杂度依赖增量序列和输入分布，删除无条件“平均约 \(O(n^{1.3})\)”的说法。
- 模拟卷完全二叉树算法改为输入规模决定的动态队列容量，补全结点计数函数和提前返回时的内存释放；LCA 题明确两个目标结点均存在且互异。

### 5.2 计算机组成原理

- 摩尔定律只用于描述集成度的历史趋势，不再把晶体管数量增长直接等同于处理器性能同比例增长。
- 删除 SRAM/存储层次中过时的固定纳秒数字，改为稳定的相对速度和容量关系。
- 存储层次不再被描述成任何实现中都严格满足“上层内容必为下层子集”；高位交叉编址不能提高的范围限定为连续单地址流。
- 流水线冒险题补充常规转发通路前提，避免在硬件条件未说明时断言某类相关“一定”阻塞。
- 修正状态/控制寄存器复用对独立可寻址寄存器和端口数量的影响。
- DMA 周期挪用不再写成“只在 CPU 不访存时偷偷传输”；DRAM 地址复用题补充行、列地址位数相同的条件；扩展操作码、Cache 和流水线综合题均重新推演。

### 5.3 操作系统

- 首次适应只保留其搜索和实现特征，不再无条件称为“综合性能最好”。
- 分页仍需检查页号、页表项有效性和访问权限；删除“不需要越界检查”的绝对说法，只说明它不做分段式段长—偏移比较。
- 中断驱动 I/O 的“一次一个字节/字”限定为 408 简化模型，实际控制器可能使用 FIFO 或突发传输。
- 原题把“SCAN（含 LOOK）”写在题干中却只解 LOOK，现已明确为 SSTF 与 LOOK。
- i-node 最大文件大小精确修正为 \(4\,\mathrm{GiB}+4\,\mathrm{MiB}+32\,\mathrm{KiB}=4,299,194,368\,\mathrm{B}\)，并修正二级间接路径与 I/O 次数解释。
- 章节末 10 道综合练习原有无答案、条件不足和过程缺失；现已全部补齐或改写，并独立复算页表、银行家、FIFO/LRU/CLOCK、磁盘调度、PV、i-node、TLB/Cache 和跨模块时间线。

### 5.4 计算机网络

- 电路交换总时延不再被缩减为“仅传播时延”，明确建立、发送和传播时延，以及它与存储转发的差异。
- CRC 检出全部单比特错误补充生成多项式的必要条件。
- IPv4 分片补充 DF=0/DF=1 边界，并明确 IPv6 中间路由器不执行分片。
- 重新区分 IGMP 与组播路由协议的职责，不再笼统声称“局域网组播不需要 IGMP”。
- “IP 地址+端口号”不再无条件称为唯一网络端点；补充传输层协议以及 TCP 四元组语境。
- 主机到本地 DNS 的递归查询改为典型教学模型/服务器策略；HTTP/1.1 明确默认持久连接，关闭连接使用 `Connection: close`。

### 5.5 模拟卷质量升级与独立复算

三套卷各替换 4 道基础直问，共新增 12 道中等或多步骤选择题：

- 卷一：Huffman WPL、Cache 地址划分、完整单级页表大小、HTTP 持久连接流水线；
- 卷二：图的边数与生成树、DDR 理论带宽、TLB 有效访问时间、GBN 信道利用率；
- 卷三：Dijkstra、理想流水线总时间、分页物理地址、子网与 ARP 联合判断。

独立复算的代表性结果包括：Huffman WPL=228，Cache Tag=19 位，TLB 有效访问时间=120 ns，GBN 利用率约 19.05%，100 条指令的 5 段流水线时间=208 ns，物理地址=5122，FIFO（3/4 页框）缺页数=16/14，LRU（4 页框）=10，CLOCK（4 页框）=14，i-node 最大字节数=67,379,200，CRC 余数为 `000`。这些复算结果均与修订后的解析一致。

选择题答案位置也做了分布审计和选项重排；只调整选项顺序并同步答案，不改变知识点。最终分布为：卷一 A/B/C/D=9/11/10/10，卷二=9/11/10/10，卷三=10/10/10/10，消除了原先正确答案明显集中于 B、D 极少的问题。

## 6. 知识覆盖矩阵

| 科目 | 已覆盖主干 | 本轮重点补充/加固 | 评价 |
|---|---|---|---|
| 数据结构 | 线性表、栈队列、串、树、图、查找、排序、算法设计 | 数组地址、特殊矩阵、Huffman、AVL、B/B+树、图算法边界 | 主干完整，算法与过程题覆盖较好 |
| 组成原理 | 系统概述、数据表示、存储、指令、CPU、总线、I/O | 字符/BCD、端序、对齐、检错纠错、移位、异常、DMA | 主干完整；个别现代硬件内容宜作为拓展阅读 |
| 操作系统 | 概述、进程、调度、同步、死锁、内存、文件、I/O | 线程模型、IPC、原子指令、管程、综合建模 | 主干完整，综合题已清除主要建模硬伤 |
| 计算机网络 | 体系结构、物理层、链路层、网络层、传输层、应用层 | ARQ、随机接入、802.11、VLAN、NAT/PAT | 主干完整，协议条件与版本边界更清楚 |

这里的“完整”是指稳定主干模块均已出现，并不意味着每个子点具有同等题量或已经逐条对照未来大纲。

## 7. 三套模拟卷审阅

### 7.1 结构核验

| 模拟卷 | 总题数 | 含解析题数 | 单选题答案数 | A/B/C/D 分布 | 结构 |
|---|---:|---:|---:|---:|---|
| 第一套 | 47 | 47 | 40 | 9/11/10/10 | 40 单选 + 7 综合 |
| 第二套 | 47 | 47 | 40 | 9/11/10/10 | 40 单选 + 7 综合 |
| 第三套 | 47 | 47 | 40 | 10/10/10/10 | 40 单选 + 7 综合 |

三套卷均按以下结构组织：

- 单项选择题 40 题 × 2 分 = 80 分；
- 综合应用题 7 题 = 70 分；
- 总分 150 分，考试时间 180 分钟；
- 数据结构 45 分、组成原理 45 分、操作系统 35 分、计算机网络 25 分。

### 7.2 质量评价

优点：

- 题目能够覆盖四科主干，不依赖外部缺失图片；
- 选择题已有唯一答案，答案数与题数一致；
- 综合题覆盖算法设计、存储/CPU 计算、进程与内存、网络协议计算；
- 大量原有数值答案和解析过程已经重新计算；
- 每套补入 4 道中等或多步骤选择题，答案位置分布也已消除明显偏斜；
- 综合题中原先缺少的算法前提、硬件模型、教材化协议模型和时空图已补齐；
- 三套卷可直接用于 180 分钟限时训练。

保留边界：

- 仍保留必要的定义辨析和单步计算题，整体是“基础—中等为主、含一定数量中等多步题”，并非刻意制作的全高难卷；
- 尚未用大规模考生作答数据校准难度、区分度和用时，不能把编辑判断等同于统计测量；
- 正式 408 每年难度和组合方式会波动，三套自编卷不能声称与任一年度真题完全等难；
- 个别拓展知识适合作为讲义补充，不宜挤占核心模拟卷题量。

后续若要把它们定位为高分冲刺卷，宜先组织限时试做，再根据正确率、区分度和平均用时定向调整，优先观察：

1. 数据结构操作过程 + 复杂度/性质联合判断；
2. 虚拟地址、TLB、Cache 和主存的跨层计算；
3. 调度、同步、页面置换或文件系统的多步骤状态演化；
4. 子网、ARP、路由、TCP 序号/窗口的跨层组合题；
5. 给出充分条件、答案唯一、无需依赖冷僻实现细节的陷阱题。

最终评价：**结构符合当前稳定试卷结构，覆盖、题风和难度分层适合作为系统复习后的阶段检测及模拟训练；难度比首轮版本更合理，但尚无统计证据支持“与正式卷完全等难”或“高难全真冲刺卷”的宣传。**

## 8. 真题元数据审计

原材料中的具体年份、题号和“直接来源于某年真题”等标注没有完成逐卷权威核验。继续保留会产生误标题源和改编题冒充原题的风险。

本轮处理如下：

1. 正文章节中的题源标签统一降级为“真题风格”；
2. 正式内容仅保留冯·诺依曼 1945、Dijkstra 1965、PCI 1992、RFC 1918 等知识史或标准编号；
3. `appendix-exams.tex` 保留在仓库中供后续审计，但从 `main.tex` 正式构建链移除；
4. 新增 `appendix-audit.tex` 说明元数据审计状态和恢复条件。

若以后恢复精确真题索引，至少需要逐题核对官方试卷或可信原卷扫描件，并记录年份、题号、题型、原始题干、改编范围和答案来源。在完成该过程前，不应对外发布精确题源归属。

## 9. 静态检查与构建结果

### 9.1 内容红旗扫描

最终扫描结果：

- 未发现 TODO、FIXME、stub、待完成、待核实、查原题、重新分析等草稿占位；
- 未发现未经隔离的精确真题年份/题号；
- 年份命中仅为知识史年份或 RFC 编号；
- 未发现“必考、每年、完全对齐、权威参照、直接来源”等强断言；
- 扫描命中的“以上都不对”“不对应”等均为正常题目或正文语义。

### 9.2 最终构建

最终连续两遍执行：

```bash
cd 408
xelatex -interaction=nonstopmode -halt-on-error main.tex
xelatex -interaction=nonstopmode -halt-on-error main.tex
```

结果：

- 两遍退出码均为 0；
- `main.pdf` 共 428 页（以当前 `fontsize=11pt` 的正式入口双遍构建为准）；
- 无致命 LaTeX 错误；
- 无未定义命令；
- 无 Missing character；
- 无重复标签；
- 第二遍无 “Label(s) may have changed”；
- 横向溢出共 251 条，绝大多数来自目录页码、代码、长公式或窄列边界；
- 无 ≥40pt 的横向溢出，最大值为 31.69785pt；
- 仍有字体形状替代、Underfull/Overfull 等非阻断排版告警，正式印刷前可继续清理，但不影响当前 PDF 生成和主体阅读。

## 10. 与当前考研 408 的适配判断

| 维度 | 判断 | 说明 |
|---|---|---|
| 科目范围 | 合适 | 四科稳定主干均已覆盖 |
| 试卷结构 | 合适 | 三套卷均为 40+7、150 分、180 分钟 |
| 科目分值 | 合适 | 每套均为 45/45/35/25 |
| 题型风格 | 合适 | 概念辨析、条件判断、计算、过程模拟、算法与综合题均有；每套已增加 4 道多步骤选择题 |
| 难度 | 基础—中等为主，含中等多步题 | 适合系统复习、阶段检测和模拟训练；尚未用考生数据标定高分区分度 |
| 知识覆盖 | 合理 | 无明显主干缺口；扩展内容应继续与核心训练区分 |
| 真题属性 | 不可作为精确索引 | 当前只能宣称“真题风格”，不能宣称逐题真题来源已核验 |
| 2027 适配 | 待新大纲发布后增量核验 | 当前只确认稳定框架，不对未发布大纲作保证 |

## 11. 剩余风险与发布门槛

### 11.1 剩余风险

1. **题源风险**：精确年份/题号仍未做权威逐卷审计。
2. **大纲时效风险**：2027 新大纲尚不能在本报告中先验确认。
3. **难度标定风险**：第二轮已经提高中等、多步骤题比例，但尚无大规模试做数据，不能精确断言与某年正式卷等难。
4. **扩展内容边界**：现代总线/硬件实例、`mmap` 等内容可帮助理解，但应避免被误认为同等优先级的核心考点。
5. **版面债务**：仍有非阻断 Underfull/Overfull 和字体替代警告；若用于印刷出版，应再做逐页视觉校样。
6. **绝对正确性边界**：即使经过本轮深审，也不应使用“零错误”“绝无问题”等无法证明的宣传语。

### 11.2 建议的正式发布口径

可以表述为：

> 本讲义覆盖截至审阅日可确认的、稳定施行的 408 四科主干与近年统考试卷结构；两轮复审确认的知识性与答案硬伤已修正，三套模拟卷均符合 40 道单选 + 7 道综合、150 分和四科分值配比，并已增加中等、多步骤选择题。题源年份尚未完成权威逐卷核验，因此章节习题以“真题风格”发布。2027 新大纲发布后将进行增量复核。

不应表述为：

- “全书零错误”；
- “所有题均为官方真题”；
- “精确覆盖 2009—2025 每一道真题”；
- “与 2027 新大纲完全一致”；
- “三套卷难度与正式考试完全等同”。

## 12. 后续优先级

1. 2027 新大纲发布后，对章节目录、术语、增删考点和模拟卷覆盖做增量对照；
2. 组织三套模拟卷的限时试做，记录正确率、区分度和平均用时，再决定是否继续增加高分区分题；
3. 如需发布真题索引，单独建立逐卷元数据核验表，不把未核验信息混入正文；
4. 对 428 页最终 PDF 做一轮逐页视觉校样，重点检查表格断页、代码换行和 251 处非阻断盒警告；
5. 后续新增或修改计算题时，保留独立复算记录，并继续执行双遍构建和静态红旗扫描。
